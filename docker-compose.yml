services:
  db:
    image: postgres:15.1-alpine
    container_name: kt-database
    environment:
      - POSTGRES_USER=${POSTGRES_USERNAME:-admin}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-admin}
      - POSTGRES_DB=${POSTGRES_DATABASE:-kt_learn}
    ports:
        - "5438:5432"
    
